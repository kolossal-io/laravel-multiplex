name: Tests

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
      - "[0-9]+.x"
  workflow_dispatch:

jobs:
  tests:
    name: Run Tests
    uses: ./.github/workflows/snippet-tests.yml
